// Yardımcı fonksiyonlar

// Aktif bildirim elementini tutacak değişken
let activeNotification = null;

// Bildirim gösterme fonksiyonu
function showNotification(message, isError = false) {
  // Fonksiyon içeriği
}

// Dışa aktarılacak fonksiyonlar
export { showNotification };
window.removeAllHighlights = removeAllHighlights;
window.removeHighlight = removeHighlight;
window.determineElementType = determineElementType;
window.findElementBySelector = findElementBySelector;
window.validateSelector = validateSelector;